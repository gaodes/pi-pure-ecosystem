# pure-utils Deprecation

`pure-utils` is deprecated.

## Successors

- Shared UI/modules: [`../pure-foundation/`](../pure-foundation/)
- Utility tools (`pi_version`, `pi_docs`, `pi_changelog`, `pi_changelog_versions`, `detect_package_manager`): `pure-dev-kit`

## Status

- Kept for historical context only.
- No new feature development.
- New shared code should be added to `pure-foundation` or `pure-dev-kit` depending on scope.
